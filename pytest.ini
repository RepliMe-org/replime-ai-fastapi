[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -m "not integration"
markers =
    integration: hits real external services (network, models, live Qdrant/LLM); deselected by default. Run with: pytest -m integration
